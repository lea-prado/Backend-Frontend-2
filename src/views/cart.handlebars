<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <h1>Mi Carrito</h1>

  {{#if cart.products.length}}
    <ul>
      {{#each cart.products}}
        <li>
          <p><strong>Producto:</strong> {{this.product.title}}</p>
          <p><strong>Cantidad:</strong> {{this.quantity}}</p>
          <p><strong>Precio Unitario:</strong> ${{this.product.price}}</p>
          <p><strong>Total:</strong> ${{multiply this.product.price this.quantity}}</p>
          <!-- Botón para eliminar este producto del carrito -->
          <form action="/api/carts/{{../cart._id}}/delete/{{this.product._id}}" method="POST" style="display:inline;">
            <button type="submit">Eliminar</button>
          </form>
        </li>
      {{/each}}
    </ul>
    <p><strong>Total a Pagar:</strong> ${{cart.total}}</p>
    <form action="/api/carts/{{cart._id}}/purchase" method="POST">
      <button type="submit">Finalizar Compra</button>
    </form>
  {{else}}
    <p>Tu carrito está vacío.</p>
  {{/if}}

  <p><a href="/">Seguir Comprando</a></p>
  <p><a href="/logout">Cerrar Sesión</a></p>
</body>
</html>
